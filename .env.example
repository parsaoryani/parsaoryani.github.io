DATABASE_URL="postgresql://postgres:postgres@localhost:5432/personal_website?schema=public"
JWT_SECRET=""  # Generate with: openssl rand -hex 32
ADMIN_PATH="x7k2-console"
# NEXT_PUBLIC_SITE_URL="https://your-production-domain.com"  # Set for production only
REVALIDATION_TOKEN=""  # Optional — for ISR webhooks

# Email (Resend)
# Sign up at https://resend.com → verify your domain → paste the API key here
RESEND_API_KEY=""
SITE_DOMAIN="parsaoryani.me"       # Your custom domain (no https://)
# Emails derived as:
#   From: contact@${SITE_DOMAIN}
#   To:   parsa@${SITE_DOMAIN}

# File storage (Cloudflare R2 — optional)
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME=""
R2_ENDPOINT=""
